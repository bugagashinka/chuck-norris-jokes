import { default as jokes } from "./jokesReducer";
import { default as categories } from "./categoriesReducer";
import { default as uiState } from "./ui";

const reducers = {
  jokes,
  categories,
  uiState,
};

export default reducers;
